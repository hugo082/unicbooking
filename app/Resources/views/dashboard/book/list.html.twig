{% extends 'dashboard/table/layout.html.twig' %}

{% block content %}
    <!-- page content -->
    <div class="right_col" role="main">
        <div class="">
            <div class="page-title">
                <div class="title_right">
                    <a href="#" class="btn btn-lg btn-success">new</a>

                </div>
            </div>

            <div class="clearfix"></div>

            <div class="row">
                <div class="col-md-12 col-sm-12 col-xs-12">
                    <div class="x_panel">
                        <div class="x_title">
                            <h2>Action Name</h2>
                            <div class="clearfix"></div>
                        </div>
                        <div class="x_content">
                            {% for book in books %}
                                {{ book.id }}
                            {% else %}
                                No book yet.
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}